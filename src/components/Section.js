import React from "react";
import '../App.css';

const Section = () => {
  return (
    <section id="work" className="section">
      <h2>What we do at wemake comes down to one thing: building without hassle. And we do that our own way. That means no unnecessary meetings about planning or processes: we are the process.</h2>
      <a href="#about" className="btn-sec-one">About us</a>
      <a href="#project-start" className="btn-sec-two">Start a project</a>
    </section>
  );
};
export default Section;
